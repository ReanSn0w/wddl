---
Название: "Замена BoltDB на JSON-хранилище очереди"
Сложность: medium
Важность: 3
---

## Что мы делаем?

Заменяем `github.com/boltdb/bolt` в `pkg/queue` на небольшую очередь в памяти, защищённую от конкурентного доступа и сохраняемую в версионированный JSON-файл. Каждое изменение очереди записывается через временный файл, `fsync` и атомарное переименование. Решение рассчитано на один экземпляр приложения, работающий с одним файлом очереди; миграция существующего `wddl.db` не требуется.

## Для чего мы делаем?

Очередь содержит мало записей и не использует преимущества встроенной key-value базы. BoltDB усложняет проект, требует отдельного бинарного формата и вызывает падение `go test -race ./...` внутри `github.com/boltdb/bolt v1.3.1` на актуальном Go/arm64. JSON-снимок сделает состояние прозрачным и уберёт устаревшую зависимость, сохранив восстановление незавершённых загрузок после перезапуска контейнера.

## План

- [ ] Зафиксировать формат JSON-файла и семантику очереди
  - [ ] Добавить целочисленную версию формата верхнего уровня.
  - [ ] Сохранять полный набор полей `engine.File`, необходимый для продолжения загрузки.
  - [ ] Представлять элементы в JSON детерминированно отсортированным списком, сохраняя быстрый поиск по ID во внутренней map.
  - [ ] Сохранить текущую семантику `Add`: повторное добавление того же ID заменяет существующую запись.
  - [ ] Сохранить текущую семантику `Delete`: удаление отсутствующего ID завершается без ошибки.
  - [ ] Считать одновременное использование файла несколькими процессами неподдерживаемым и явно описать это ограничение.

- [ ] Переписать инициализацию `pkg/queue`
  - [ ] При существующем JSON-файле полностью прочитать его до публикации очереди вызывающему коду.
  - [ ] Проверить версию формата и обязательные данные каждой записи.
  - [ ] Отклонять повторяющиеся ID и несовместимую версию с понятной ошибкой.
  - [ ] При отсутствии файла создать пустое состояние без попытки миграции `wddl.db`.
  - [ ] При пустом или повреждённом JSON завершать запуск с ошибкой, не перезаписывая исходный файл.
  - [ ] Проверять доступность родительского каталога для последующего сохранения.

- [ ] Реализовать потокобезопасное состояние очереди в памяти
  - [ ] Защитить map записей через `sync.RWMutex`.
  - [ ] Обслуживать `Exists`, `Len`, `Stat` и `List` из согласованного снимка памяти.
  - [ ] Не выполнять пользовательский фильтр `List` под удерживаемой блокировкой, чтобы исключить дедлок и длительное блокирование изменений.
  - [ ] Возвращать из `List` копии значений, не раскрывая изменяемое внутреннее состояние.
  - [ ] Сохранять корректное конкурентное поведение `Add`, `Delete`, чтений и `Chan` под `go test -race`.

- [ ] Добавить надёжную запись JSON-снимков
  - [ ] Для каждой мутации сначала подготовить новое состояние, не изменяя опубликованную map.
  - [ ] Создавать временный файл в том же каталоге, что и целевой файл очереди.
  - [ ] Устанавливать права файла `0600`.
  - [ ] Кодировать снимок в читаемый JSON с устойчивым порядком элементов.
  - [ ] Выполнять `Sync` временного файла перед его закрытием.
  - [ ] Атомарно переименовывать полностью записанный временный файл в целевой.
  - [ ] Синхронизировать родительский каталог после переименования на поддерживаемых платформах.
  - [ ] Публиковать новое состояние в памяти только после успешного сохранения на диск.
  - [ ] Удалять временный файл после ошибки, не повреждая последний успешный JSON-снимок.

- [ ] Адаптировать выдачу задач через `Queue.Chan`
  - [ ] Сразу выдавать актуальный снимок очереди после запуска подписчика, не ожидая первого трёхсекундного тика.
  - [ ] Добавить внутреннее уведомление о новых или изменённых задачах для немедленной выдачи.
  - [ ] Сохранить периодическую повторную выдачу оставшихся задач, чтобы неуспешная загрузка могла быть запущена повторно без новой мутации JSON.
  - [ ] Применять переданный фильтр к каждому актуальному снимку.
  - [ ] Прерывать ожидание и отправку элементов при отмене `context.Context`.
  - [ ] Не блокировать `Add` или `Delete`, если получатель канала временно не читает задачи.

- [ ] Обновить конфигурацию и контейнерный запуск
  - [ ] Заменить понятие `DB_FILE` в `cmd/webdav/main.go` на путь к файлу очереди с именем, соответствующим JSON-формату.
  - [ ] Выбрать значение по умолчанию `./queue.json` до последующего переноса конфигурации в YAML.
  - [ ] Обновить `example.env` новым параметром пути.
  - [ ] Обновить `Dockerfile` новым параметром пути.
  - [ ] Добавить `queue.json` и временные файлы атомарной записи в `.gitignore`, не исключая все JSON-файлы проекта.
  - [ ] Описать необходимость размещать файл очереди на постоянном volume, если очередь должна переживать пересоздание контейнера.

- [ ] Удалить BoltDB из проекта
  - [ ] Удалить импорт и весь код работы с bucket/transaction из `pkg/queue`.
  - [ ] Удалить `github.com/boltdb/bolt` из `go.mod` и `go.sum` через `go mod tidy`.
  - [ ] Проверить, что в исходниках и документации не осталось активных ссылок на `wddl.db`, `DB_FILE` или BoltDB.
  - [ ] Не добавлять код автоматической миграции старой базы.

- [ ] Переписать тесты `pkg/queue`
  - [ ] Проверить запуск с отсутствующим файлом очереди.
  - [ ] Проверить загрузку корректного JSON и сохранение всех полей `engine.File`.
  - [ ] Проверить отказ при пустом файле.
  - [ ] Проверить отказ при повреждённом JSON.
  - [ ] Проверить отказ при неизвестной версии формата.
  - [ ] Проверить добавление новой записи.
  - [ ] Проверить замену записи с существующим ID.
  - [ ] Проверить удаление существующей и отсутствующей записи.
  - [ ] Проверить `Exists`, `Len`, `Stat` и фильтрацию `List`.
  - [ ] Проверить сохранение последнего успешного файла при искусственной ошибке записи.
  - [ ] Проверить отсутствие оставшихся временных файлов после ошибки.
  - [ ] Проверить немедленную выдачу новой задачи через `Chan`.
  - [ ] Проверить повторную выдачу задачи после неуспешного цикла загрузки.
  - [ ] Проверить завершение `Chan` после отмены контекста.
  - [ ] Добавить конкурентный тест одновременных чтений, добавлений и удалений.

- [ ] Обновить документацию проекта
  - [ ] Описать назначение JSON-файла очереди в `README.md`.
  - [ ] Указать, что файл можно удалить только при остановленном контейнере и что очередь затем восстановится после WebDAV-сканирования.
  - [ ] Уточнить, что каталоги незавершённых частей в `TEMP` не зависят от формата очереди и продолжают использовать прежний ID файла.
  - [ ] Зафиксировать ограничение одного процесса на один файл очереди в `AGENTS.md`.
  - [ ] Удалить из `AGENTS.md` известное ограничение race detector, если полный race-прогон станет успешным.

- [ ] Проверить готовое изменение
  - [ ] Отформатировать изменённые Go-файлы через `gofmt`.
  - [ ] Выполнить `go mod tidy` и проверить ожидаемый diff зависимостей.
  - [ ] Выполнить `go test ./...`.
  - [ ] Выполнить `go test -race ./...`.
  - [ ] Выполнить `go vet ./...`.
  - [ ] Собрать CLI командой `go build -o /tmp/wddl ./cmd/webdav`.
  - [ ] Перезапустить тестовый контейнер с сохранённым `queue.json` и подтвердить восстановление незавершённой задачи.
  - [ ] Удалить тестовый `queue.json` при остановленном контейнере и подтвердить восстановление задачи следующим WebDAV-сканированием.
