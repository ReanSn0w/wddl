---
Название: "Alpine runtime и multi-arch Docker-образ"
Сложность: medium
Важность: 2
Дата завершения: 2026-09-13
---

## Что мы делаем?

Переводим финальный Docker-образ со `scratch` на закреплённую поддерживаемую версию Alpine, устанавливаем бинарник как `/usr/local/bin/wddl`, запускаем контейнер командой `wddl run` от непривилегированного пользователя и документируем безопасную подстановку UID/GID в Synology Compose. Обновляем CI для публикации одного GHCR manifest с вариантами `linux/amd64` и `linux/arm64`: текущий Synology NAS использует `amd64`, а ARM64-вариант сохраняется для переносимости образа на другие устройства.

## Для чего мы делаем?

Текущий scratch-образ минимален, но не содержит shell, а приложение расположено как `/srv/app`, что неудобно для вызова новых управляющих команд через интерфейс Synology. Текущий GitHub Actions workflow уже создаёт подходящий для имеющегося `amd64` NAS образ, однако публикация дополнительного ARM64-варианта делает релиз переносимым без отдельных тегов. Alpine добавит `/bin/sh` и базовые BusyBox-утилиты, а размещение `wddl` в стандартном `PATH` позволит вызывать команды из любой директории. Запуск без root и явные ограничения контейнера компенсируют увеличение runtime-поверхности.

## План

- [x] Перестроить многостадийный Dockerfile
  - [x] Закрепить поддерживаемые minor-версии Go builder image и Alpine runtime image вместо плавающего `latest`.
  - [x] Синхронизировать версию Go builder с директивой `go` в `go.mod`.
  - [x] Использовать `COPY` для `go.mod` и `go.sum` отдельным слоем до копирования исходников.
  - [x] Загружать Go-модули отдельным кэшируемым шагом сборки.
  - [x] Использовать аргументы BuildKit `BUILDPLATFORM`, `TARGETOS` и `TARGETARCH` для кросс-компиляции.
  - [x] Собирать бинарник с `CGO_ENABLED=0`, `-trimpath` и существующим внедрением revision через `-ldflags`.
  - [x] Копировать готовый бинарник в `/usr/local/bin/wddl` финального образа.
  - [x] Установить в runtime только `ca-certificates`, сохранив встроенные `/bin/sh` и BusyBox-утилиты Alpine.
  - [x] Не устанавливать Bash, curl, компилятор или пакетный кэш без отдельной эксплуатационной необходимости.

- [x] Настроить стандартный запуск CLI внутри контейнера
  - [x] Использовать exec-form `ENTRYPOINT ["wddl"]` без промежуточного shell.
  - [x] Использовать `CMD ["run"]` для запуска демона по умолчанию.
  - [x] Сохранить возможность переопределить CMD для автономных команд `version` и `config validate`.
  - [x] Проверить, что `wddl`, `wddl --help` и вложенные команды находятся через `PATH` из любой рабочей директории.
  - [x] Установить стабильный `WORKDIR` в записываемом каталоге состояния, согласованном с YAML-примером.
  - [x] Удалить старый путь `/srv/app` и не оставлять вторую копию бинарника.

- [x] Добавить безопасного пользователя по умолчанию
  - [x] Создать в Alpine системные группу и пользователя `wddl` с закреплёнными числовыми UID/GID.
  - [x] Переключить финальный образ на `USER wddl:wddl`.
  - [x] Создать только небольшие служебные каталоги образа и назначить их пользователю `wddl` на этапе сборки.
  - [x] Не выполнять рекурсивный `chown` примонтированных download/library каталогов при старте контейнера.
  - [x] Не запускать entrypoint от root ради динамического изменения UID/GID.
  - [x] Обеспечить понятную ошибку приложения, если настроенные output/temp/state каталоги недоступны на запись.

- [x] Поддержать UID/GID владельца каталогов Synology через Compose
  - [x] Добавить в пример Compose поле `user: "${WDDL_UID:-10001}:${WDDL_GID:-10001}"` с дефолтами, совпадающими с пользователем образа.
  - [x] Пояснить, что Compose не выполняет `$(id -u)` или `$(id -g)` внутри YAML.
  - [x] Описать ввод числовых `WDDL_UID` и `WDDL_GID` в окружении проекта Synology или его `.env`.
  - [x] Не передавать `WDDL_UID` и `WDDL_GID` внутрь приложения, поскольку они нужны Compose только для выбора пользователя процесса.
  - [x] Проверить доступ выбранного UID/GID на чтение WebDAV-конфига и дополнительных библиотек.
  - [x] Проверить доступ выбранного UID/GID на запись в output, temp и каталог состояния.
  - [x] Предупредить, что произвольный numeric UID может не иметь имени в `/etc/passwd`, но это не мешает выполнению `wddl` и `/bin/sh`.

- [x] Разделить контейнерные точки монтирования по назначению
  - [x] Выделить read-only mount для `config.yaml`.
  - [x] Монтировать дополнительные локальные библиотеки только с `:ro`.
  - [x] Выделить writable mount для конечных загрузок.
  - [x] Выделить writable mount для временных частей.
  - [x] Выделить writable mount для `queue.json` и Unix-сокета либо документировать их размещение в общем state-каталоге.
  - [x] Не объявлять пользовательские медиакаталоги через жёстко заданные инструкции `VOLUME` в Dockerfile.
  - [x] Согласовать container paths в Compose-примере с `config.example.yaml`.

- [x] Усилить runtime-ограничения в рекомендуемом Compose
  - [x] Добавить `read_only: true` для корневой файловой системы контейнера.
  - [x] Добавить небольшой writable `tmpfs` для `/tmp`, если он требуется Alpine или диагностическим командам.
  - [x] Добавить `cap_drop: [ALL]`.
  - [x] Добавить `security_opt: [no-new-privileges:true]`.
  - [x] Не публиковать управляющий Unix-сокет как TCP-порт.
  - [x] Не использовать `privileged: true` и не монтировать Docker socket.
  - [x] Проверить исходящее HTTPS-соединение с WebDAV при полностью удалённых Linux capabilities.

- [x] Удалить устаревшие настройки runtime-образа
  - [x] Удалить встроенные гостевые значения `WEBDAV_USER` и `WEBDAV_PASSWORD`.
  - [x] Удалить старые `ENV INPUT`, `TEMP`, `OUTPUT`, `THREADS`, `TIMEOUT` и `DB_FILE` после выполнения YAML-плана.
  - [x] Оставить только нейтральное значение `WDDL_CONFIG`, если для контейнера нужен путь по умолчанию.
  - [x] Удалить старую инструкцию `VOLUME ["/data"]`, если она конфликтует с явными mounts в Compose-документации.
  - [x] Проверить отсутствие credentials и пользовательских путей во всех слоях итогового образа.

- [x] Добавить container healthcheck через CLI
  - [x] Использовать `wddl status --json` как проверку доступности демона через Unix-сокет.
  - [x] Настроить разумные interval, timeout и start period, учитывая первоначальное сканирование локальной библиотеки.
  - [x] Не считать временную ошибку отдельной загрузки причиной нездорового состояния всего контейнера.
  - [x] Возвращать unhealthy при недоступном control socket или остановленном демоне.
  - [x] Проверить healthcheck без curl и других дополнительных runtime-пакетов.

- [x] Обновить GitHub Actions до актуальной версии Go и основной ветки
  - [x] Установить в test job версию Go, совместимую с `go.mod`, вместо текущей Go 1.21.
  - [x] Выполнить `go test ./...` перед сборкой образов.
  - [x] Выполнить `go vet ./...` перед публикацией.
  - [x] Исправить условие публикации с несуществующей основной ветки `master` на `main`.
  - [x] Сохранить публикацию semver-тегов `v*` в GHCR.
  - [x] Обновить используемые GitHub Actions до поддерживаемых major-версий после проверки их release notes.

- [x] Настроить multi-arch сборку и публикацию
  - [x] Инициализировать QEMU до Buildx для эмулируемой ARM64-сборки на x86 runner.
  - [x] Инициализировать отдельный Docker Buildx builder.
  - [x] Передать `platforms: linux/amd64,linux/arm64` в build-push action.
  - [x] Передавать одинаковый revision build argument во все платформенные варианты.
  - [x] Публиковать единый manifest list для каждого branch/semver-тега вместо архитектурных тегов вручную.
  - [x] Включить GitHub Actions cache для слоёв Go modules и Docker build.
  - [x] Не пытаться загружать multi-arch manifest целиком в локальное image store стандартного runner.
  - [x] После публикации проверить наличие обеих платформ через `docker buildx imagetools inspect`.

- [x] Добавить проверки Dockerfile и образов
  - [x] Собрать локальный образ для нативной архитектуры разработчика.
  - [x] Проверить `wddl version` внутри образа без запуска демона.
  - [x] Проверить дерево `wddl --help` внутри образа.
  - [x] Проверить наличие `/bin/sh` и базовых BusyBox-команд.
  - [x] Проверить отсутствие `/srv/app`.
  - [x] Проверить пользователя процесса через `id` внутри контейнера.
  - [x] Проверить запись в настроенные writable mounts от стандартного пользователя образа.
  - [x] Проверить отказ записи в read-only библиотеку.
  - [x] Проверить запуск с `read_only`, `cap_drop: ALL` и `no-new-privileges`.
  - [x] Проверить graceful shutdown команды `run` по `SIGTERM`.
  - [x] Проверить выбор AMD64-варианта опубликованного образа на Synology NAS.
  - [x] Проверить запуск ARM64-варианта в CI или другой доступной ARM64-среде.

- [x] Обновить документацию эксплуатации
  - [x] Добавить готовый Compose-пример для Synology с единым multi-arch тегом образа.
  - [x] Описать настройку `WDDL_UID` и `WDDL_GID` числовыми значениями.
  - [x] Описать права каждого read-only и writable mount.
  - [x] Показать вызов `wddl status`, `wddl watch` и `wddl scan remote` через интерфейс контейнера Synology.
  - [x] Уточнить, что Alpine предоставляет `/bin/sh`, но не Bash и не curl.
  - [x] Описать использование healthcheck и его границы.
  - [x] Обновить Docker-раздел `AGENTS.md` и удалить описание scratch runtime.

- [x] Проверить готовое изменение
  - [x] Выполнить `go test ./...`.
  - [x] Выполнить `go test -race ./...` после замены BoltDB.
  - [x] Выполнить `go vet ./...`.
  - [x] Собрать `linux/amd64` и `linux/arm64` без публикации.
  - [x] Сравнить размер Alpine-образа с последним scratch-образом и зафиксировать ожидаемое увеличение в описании релиза.
  - [x] Просмотреть историю слоёв и убедиться в отсутствии credentials.
  - [x] Запустить полный Compose-пример на тестовых каталогах.
  - [x] Проверить управляющие команды через Unix-сокет от настроенного Synology UID/GID.
  - [x] Выполнить тестовую загрузку по HTTPS на AMD64 Synology NAS.
