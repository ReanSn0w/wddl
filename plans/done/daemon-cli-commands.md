---
Название: "Команды управления демоном через Unix-сокет"
Сложность: hard
Важность: 1
Дата завершения: 2026-09-13
---

## Что мы делаем?

Разделяем `wddl` на команду `run`, запускающую единственный демон загрузки, и набор клиентских команд управления. Демон становится единственным владельцем WebDAV-клиента, очереди, локального индекса и активных загрузок, а команды обращаются к нему через локальный Unix domain socket по версионированному HTTP/JSON-протоколу. Добавляем ручной запуск сканирования, статус, поток событий, управление отдельными задачами, безопасную очистку WebDAV, проверку и reload YAML, расчёт ID удалённого файла и машиночитаемый вывод. Одновременно удаляем `git.papkovda.ru/library/gokit`, используем `go-flags` напрямую и сохраняем `github.com/go-pkgz/lgr`.

## Для чего мы делаем?

Сейчас приложение запускает только один долгоживущий режим, а повлиять на него можно лишь перезапуском контейнера или отдельным процессом очистки. Из-за этого удалённое сканирование приходится выполнять чаще необходимого, прогресс загрузок засоряет постоянный лог, а служебные действия не координируются с работающим движком. Локальный сокет позволит запускать редкие операции из интерфейса Synology без TCP-порта, повторной авторизации в WebDAV и конкурентного открытия JSON-очереди.

## План

- [x] Зафиксировать дерево команд и общий контракт CLI
  - [x] Добавить `run` для запуска демона.
  - [x] Добавить `status` и `watch` для наблюдения за демоном.
  - [x] Добавить `scan remote`, `scan local` и `scan all` для досрочного сканирования.
  - [x] Добавить `queue list`, `queue remove <id>` и `queue retry <id>` для управления задачами.
  - [x] Добавить `download cancel <id>` для отмены активной загрузки с сохранением готовых частей.
  - [x] Добавить `id <remote-path>` для расчёта ID удалённого файла.
  - [x] Добавить `cleanup remote` с предварительным просмотром и отдельным подтверждением.
  - [x] Добавить `config validate` и `config reload`.
  - [x] Добавить `version`.
  - [x] Поддержать `--json` для всех команд, возвращающих данные.
  - [x] Возвращать ненулевой exit code для ошибок разбора, недоступного демона, отклонённой операции и частично неуспешной очистки.

- [x] Перевести CLI на прямое использование `github.com/umputun/go-flags`
  - [x] Сделать `go-flags` прямой зависимостью и описать команды отдельными типизированными структурами.
  - [x] Добавить понятные descriptions, positional arguments и примеры в `--help` каждой команды.
  - [x] Запретить неизвестные команды и флаги вместо текущего `IgnoreUnknown`.
  - [x] Сделать отсутствие команды ошибкой при ручном запуске бинарника.
  - [x] Настроить Dockerfile с `ENTRYPOINT` на бинарник и `CMD ["run"]`, чтобы контейнер по умолчанию запускал демон.
  - [x] Разделить парсинг аргументов, выполнение команды и выбор exit code, не вызывая `os.Exit` из внутренних пакетов.

- [x] Удалить зависимость от `gokit`
  - [x] Заменить `app.Context()` на корневой контекст из `signal.NotifyContext` для `SIGINT` и `SIGTERM`.
  - [x] Реализовать локальную координацию graceful shutdown с настраиваемым таймаутом.
  - [x] Сохранить внедрение revision через `-ldflags` и вывод версии сборки.
  - [x] Использовать `lgr` напрямую для постоянного логирования.
  - [x] Удалить импорт `git.papkovda.ru/library/gokit/pkg/app` из `cmd/webdav/main.go`.
  - [x] Удалить модуль `git.papkovda.ru/library/gokit` через `go mod tidy` после замены всех его возможностей.

- [x] Добавить конфигурацию локального control socket в YAML
  - [x] Расширить схему из `plans/yaml-configuration.md` секцией `control`.
  - [x] Добавить `control.socket` со значением по умолчанию `/run/wddl/wddl.sock`.
  - [x] Добавить `control.request_timeout` со значением по умолчанию `30s` для обычных клиентских запросов.
  - [x] Добавить `control.shutdown_timeout` со значением по умолчанию `10s`.
  - [x] Валидировать абсолютный путь сокета и положительные duration.
  - [x] Использовать тот же `--config` или `WDDL_CONFIG` в клиентских командах для обнаружения сокета.
  - [x] Не требовать WebDAV credentials от клиентского процесса.

- [x] Реализовать безопасный жизненный цикл Unix-сокета
  - [x] Создавать родительский каталог сокета при запуске демона.
  - [x] Перед удалением существующего socket-файла проверять попыткой подключения, не работает ли уже другой демон.
  - [x] Завершать запуск с ошибкой, если существующий сокет отвечает.
  - [x] Удалять только подтверждённо устаревший socket-файл.
  - [x] Ограничить файловые права сокета владельцем и группой контейнера.
  - [x] Не открывать TCP listener и не публиковать управляющий API в сеть.
  - [x] Прекращать принимать запросы в начале graceful shutdown.
  - [x] Удалять socket-файл после остановки HTTP-сервера.

- [x] Определить версионированный HTTP/JSON-протокол управления
  - [x] Разместить endpoints под префиксом `/v1`.
  - [x] Ввести единый JSON-формат успешного ответа.
  - [x] Ввести единый JSON-формат ошибки с машинным кодом и понятным сообщением.
  - [x] Сопоставить ошибки валидации, отсутствующие задачи, конфликты состояния и внутренние ошибки с устойчивыми HTTP status codes.
  - [x] Ограничить размер request body и установить серверные таймауты.
  - [x] Не передавать через API WebDAV credentials или полный эффективный конфиг с секретами.
  - [x] Сохранить обратную совместимость внутри версии `/v1` после релиза `v1.0`.

- [x] Превратить движок в управляемый долгоживущий сервис
  - [x] Сделать демон единственным владельцем `Engine`, JSON-очереди, WebDAV-клиента и локального индекса.
  - [x] Ввести потокобезопасный runtime state для времени запуска, состояния сканеров, очереди, активных загрузок и последней ошибки подсистем.
  - [x] Выполнять начальное сканирование локальных библиотек до первого удалённого сканирования.
  - [x] Запускать первое удалённое сканирование сразу после успешной инициализации.
  - [x] Направить периодические и ручные сканирования через единый scheduler.
  - [x] Завершать scheduler, активные HTTP handlers и воркеры по корневому контексту.
  - [x] Не позволять клиентским командам напрямую открывать файл очереди или создавать второй WebDAV-клиент.

- [x] Реализовать ручные триггеры сканирования
  - [x] Поставить для remote и local scan отдельные буферизованные сигналы ёмкостью один.
  - [x] Сразу отвечать клиенту, что запрос принят или уже запланирован.
  - [x] Объединять повторные запросы одного типа, не запуская параллельные обходы.
  - [x] Не запускать второй scan, если такой обход уже выполняется.
  - [x] Для `scan all` сначала обновлять локальный индекс, затем запускать WebDAV-сканирование.
  - [x] Отражать запланированное, активное и завершённое сканирование в `status` и `watch`.
  - [x] Сохранять расписание следующего периодического запуска после ручного триггера без создания плотного цикла сканирования.

- [x] Реализовать команду `status`
  - [x] Показывать uptime и состояние graceful shutdown.
  - [x] Показывать количество ожидающих, suspended и активных задач.
  - [x] Показывать имена, полные ID, размеры и прогресс активных загрузок.
  - [x] Показывать среднюю текущую скорость и оценку оставшегося времени при доступных данных.
  - [x] Показывать состояние remote/local scan, время последнего результата и время следующего планового запуска.
  - [x] Показывать количество файлов в активном локальном индексе.
  - [x] Показывать последнюю ошибку каждой подсистемы без credentials.
  - [x] Версионировать JSON-представление статуса для использования в автоматизации и healthcheck.

- [x] Перенести детальный прогресс из логов в `watch`
  - [x] Добавить неблокирующий broker runtime-событий внутри демона.
  - [x] Публиковать события постановки задачи, запуска, прогресса, завершения, ошибки, отмены и возобновления загрузки.
  - [x] Публиковать события планирования, запуска и результата сканирований.
  - [x] Публиковать события reload и cleanup.
  - [x] Передавать поток через Unix-сокет в формате NDJSON для `watch --json`.
  - [x] Рендерить человекочитаемый поток по умолчанию.
  - [x] Не позволять медленному или отключившемуся подписчику блокировать движок.
  - [x] Сообщать клиенту о пропущенных из-за переполнения буфера событиях.
  - [x] Убрать регулярное логирование каждого сообщения `Progress`, сохранив в логах запуск, итог, предупреждения и ошибки.

- [x] Добавить управление задачами по ID
  - [x] Показывать ID в `status`, `watch` и `queue list`.
  - [x] Принимать полный ID или однозначный префикс ID в управляющих командах.
  - [x] Возвращать ошибку со списком совпадений для неоднозначного префикса.
  - [x] Сортировать человекочитаемый и JSON-вывод `queue list` детерминированно.
  - [x] Реализовать `queue remove` как удаление задачи из очереди без удаления удалённого файла и временных частей.
  - [x] Предупреждать, что удалённый scan может снова добавить удалённую через `queue remove` задачу.
  - [x] Отклонять `queue remove` для активной загрузки и предлагать сначала выполнить `download cancel`.
  - [x] Расширить запланированный JSON-формат очереди состояниями `ready` и `suspended`.
  - [x] Реализовать `queue retry` как перевод suspended-задачи в `ready` и немедленный сигнал scheduler.

- [x] Сделать отдельную загрузку отменяемой
  - [x] Передавать отдельный дочерний `context.Context` в каждый вызов `Downloader.Download`.
  - [x] Хранить cancel function активной загрузки в runtime state до её завершения.
  - [x] Прерывать чтение WebDAV и локальную запись после отмены без публикации конечного файла.
  - [x] Синхронизировать и закрывать текущую часть перед завершением отменённой задачи.
  - [x] Сохранять полностью готовые части для последующего продолжения.
  - [x] Переводить отменённую задачу в `suspended`, чтобы текущий scheduler не запустил её снова.
  - [x] Возвращать ошибку, если указанный ID не относится к активной загрузке.

- [x] Реализовать `id <remote-path>` без изменения текущей формулы ID
  - [x] Принимать только путь WebDAV, а не локальный путь контейнера.
  - [x] Нормализовать удалённый путь относительно настроенного `webdav.root` без использования `filepath`.
  - [x] Запрашивать актуальный размер удалённого файла через WebDAV внутри демона.
  - [x] Отклонять отсутствующий путь и путь каталога.
  - [x] Вычислять ID существующим `engine.NewFile` из полного удалённого пути и размера.
  - [x] Возвращать тип, нормализованный путь, имя, точный размер и полный ID.
  - [x] Не читать содержимое удалённого файла для расчёта ID.

- [x] Реализовать безопасный двухэтапный `cleanup remote`
  - [x] На первом запросе построить список только тех удалённых файлов, для которых существует подтверждённая локальная копия.
  - [x] Показать полный список путей, количество файлов и суммарный размер удаления.
  - [x] Создать криптографически случайный одноразовый токен, связанный с точным снимком кандидатов.
  - [x] Установить короткий срок действия токена и хранить его только в памяти демона.
  - [x] Не выполнять удаление первым запросом даже при неинтерактивном запуске.
  - [x] Печатать готовую команду `cleanup remote --confirm <token>` для интерфейса Synology.
  - [x] Перед каждым `DELETE` повторно проверить локальный путь, имя и размер.
  - [x] Перед каждым `DELETE` повторно проверить актуальные метаданные удалённого файла.
  - [x] Пропускать изменившиеся или исчезнувшие после preview элементы.
  - [x] Сделать токен одноразовым независимо от полного или частичного успеха операции.
  - [x] Сериализовать cleanup с другими операциями удаления на WebDAV.
  - [x] Вернуть итоговые количества удалённых, пропущенных и ошибочных элементов.

- [x] Реализовать `config validate` и безопасный `config reload`
  - [x] Выполнять `config validate` локально без соединения с сокетом и без проверки credentials.
  - [x] Возвращать нормальный exit code и JSON-результат успешной или неуспешной валидации.
  - [x] Загружать и строго валидировать новый YAML до отправки reload в runtime-компоненты.
  - [x] Разрешить reload интервалов remote/local scan и уровня debug-логирования.
  - [x] Отклонять reload целиком при изменении WebDAV server/root, output/temp/queue/socket, числа воркеров, `remove_remote` или списка локальных корней.
  - [x] Возвращать список полей, требующих перезапуска, не применяя остальные изменения частично.
  - [x] Атомарно публиковать разрешённые настройки после успешного обновления scheduler и logger.
  - [x] Отражать время и результат последнего reload в `status`.

- [x] Добавить автономную команду `version`
  - [x] Выводить revision, версию Go и целевую платформу без запуска демона.
  - [x] Поддержать устойчивый JSON-формат версии.
  - [x] Сохранять работоспособность `version` без YAML и credentials.

- [x] Покрыть CLI и управляющий API тестами
  - [x] Проверить дерево `--help`, обязательные аргументы и отказ для неизвестной команды.
  - [x] Проверить выбор `run` через Docker `CMD` отдельно от поведения CLI без команды.
  - [x] Проверить создание, обнаружение занятого и удаление устаревшего Unix-сокета.
  - [x] Проверить права socket-файла и отсутствие TCP listener.
  - [x] Проверить JSON success/error envelopes и ограничение request body.
  - [x] Проверить немедленный ответ и объединение повторных scan-запросов.
  - [x] Проверить порядок local → remote для `scan all`.
  - [x] Проверить согласованный human/JSON status.
  - [x] Проверить, что медленный `watch` не блокирует публикацию событий.
  - [x] Проверить разрешение полного и уникального сокращённого ID.
  - [x] Проверить отказ для неоднозначного сокращённого ID.
  - [x] Проверить `queue remove`, повторное обнаружение и `queue retry`.
  - [x] Проверить отмену активной загрузки, сохранение частей и состояние `suspended`.
  - [x] Проверить расчёт ID существующего удалённого файла и ошибки для каталога/отсутствующего пути.
  - [x] Проверить невозможность cleanup без действующего confirm-токена.
  - [x] Проверить истечение и одноразовость confirm-токена.
  - [x] Проверить повторную локальную и удалённую валидацию перед cleanup.
  - [x] Проверить атомарный отказ reload при изменении неизменяемого поля.
  - [x] Проверить graceful shutdown с активными клиентами и загрузками.
  - [x] Выполнить интеграционные тесты клиента и сервера через временный Unix-сокет без настоящего WebDAV.

- [x] Обновить документацию и контейнерный сценарий Synology
  - [x] Описать каждую команду и привести сокращённые примеры в `README.md`.
  - [x] Описать запуск клиентской команды внутри уже работающего контейнера через интерфейс Synology.
  - [x] Описать различие между постоянными логами, снимком `status` и потоком `watch`.
  - [x] Описать двухэтапное подтверждение cleanup без интерактивного терминала.
  - [x] Описать полный ID, сокращённые префиксы и команду `id`.
  - [x] Обновить схему YAML и `config.example.yaml` секцией `control`.
  - [x] Обновить `AGENTS.md` новой точкой входа, архитектурой демона и правилами изменения control API.

- [x] Проверить готовое изменение
  - [x] Отформатировать изменённые Go-файлы через `gofmt`.
  - [x] Выполнить `go mod tidy` и проверить удаление `gokit` при сохранении прямых зависимостей `go-flags` и `lgr`.
  - [x] Выполнить `go test ./...`.
  - [x] Выполнить `go test -race ./...` после замены BoltDB.
  - [x] Выполнить `go vet ./...`.
  - [x] Собрать CLI командой `go build -o /tmp/wddl ./cmd/webdav`.
  - [x] Проверить справку всех команд из собранного бинарника.
  - [x] Запустить тестовый контейнер командой по умолчанию `run`.
  - [x] Вызвать `status`, `watch`, все варианты `scan`, `id` и `config reload` через интерфейс Synology.
  - [x] Выполнить preview и подтверждение cleanup только на тестовом WebDAV-каталоге.
  - [x] Отправить контейнеру `SIGTERM` во время активной загрузки и подтвердить корректное завершение без повреждения конечного файла.
