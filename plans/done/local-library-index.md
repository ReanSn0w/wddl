---
Название: "Проверка файлов в дополнительных локальных библиотеках"
Сложность: medium
Важность: 2
Дата завершения: 2026-09-13
---

## Что мы делаем?

Добавляем поддержку нескольких примонтированных в контейнер read-only каталогов с уже существующими файлами. При запуске утилита рекурсивно строит в памяти индекс по точной паре «имя файла + размер», затем полностью обновляет его с настраиваемым интервалом. Перед постановкой удалённого файла в очередь и непосредственно перед загрузкой утилита проверяет индекс, чтобы не создавать дубликаты при отличающейся структуре каталогов. Если включён `CLEAR_REMOTE`, найденный удалённый файл удаляется только после повторной проверки реального локального пути и размера.

## Для чего мы делаем?

Сейчас движок умеет распознавать готовый файл только по ожидаемому пути внутри `OUTPUT`. Видео, уже перемещённые в другие библиотеки, скачиваются повторно, потому что их фактические локальные пути отличаются от WebDAV-путей. При объёме библиотек около 7 ТБ и примерно 1500 файлов рекурсивный сбор метаданных при запуске и раз в сутки достаточно дешёв, а небольшой индекс в памяти позволяет выполнять последующие проверки без повторного обхода дисков. Повторный `stat` перед удалением защищает от устаревшей записи, если локальный файл после индексации был удалён, переименован или изменён.

## План

- [x] Определить контракт поиска существующего локального файла
  - [x] Добавить интерфейс проверки по точному регистрозависимому имени и размеру, не связывая `pkg/engine` с конкретной реализацией индекса.
  - [x] Возвращать из проверки конкретный подтверждённый локальный путь, чтобы решение об удалении не основывалось только на ключе индекса.
  - [x] Сохранить текущее поведение без дополнительного поиска, если список библиотек пуст.
  - [x] Зафиксировать обработку нескольких файлов с одинаковыми именем и размером: совпадение действительно, пока существует хотя бы один подходящий путь.

- [x] Добавить временную конфигурацию дополнительных библиотек в `cmd/webdav/main.go`
  - [x] Добавить CLI-параметр и переменную окружения со списком путей внутри контейнера, игнорируя пустые элементы и пробелы вокруг них.
  - [x] Добавить интервал полного обновления индекса с безопасным значением по умолчанию 24 часа.
  - [x] Проверять при запуске, что каждый настроенный корень существует, является каталогом и доступен для чтения.
  - [x] Завершать запуск с понятной ошибкой, если первоначальный индекс нельзя построить полностью.

    Эти параметры являются переходными до отдельного плана миграции конфигурации в YAML.

- [x] Реализовать потокобезопасный индекс локальных библиотек в отдельном пакете
  - [x] Рекурсивно обходить все настроенные корни без чтения содержимого файлов.
  - [x] Индексировать обычные файлы по ключу из полного имени с учётом регистра и точного размера в байтах.
  - [x] Хранить для одного ключа все найденные пути, чтобы удаление одного дубликата не скрывало другой.
  - [x] Строить новый снимок отдельно и атомарно подменять активный индекс только после успешного полного обхода всех корней.
  - [x] При периодической ошибке обхода оставлять предыдущий снимок активным и логировать причину отказа обновления.
  - [x] Перед положительным ответом повторно выполнять `os.Stat` найденных путей и сверять тип файла, точное имя и размер.
  - [x] Удалять из активного индекса обнаруженные устаревшие пути, не затрагивая остальные совпадения того же ключа.

- [x] Подключить жизненный цикл индекса к приложению
  - [x] Выполнять первое полное сканирование синхронно до запуска удалённого сканера и воркеров загрузки.
  - [x] Запускать последующие полные обновления по настроенному интервалу.
  - [x] Останавливать фоновое обновление при завершении `context.Context` приложения.
  - [x] Логировать начало и результат сканирования, количество проиндексированных файлов и продолжительность без вывода полного списка путей.

- [x] Использовать единый поиск локальной копии в `pkg/engine`
  - [x] Сначала сохранять быструю проверку ожидаемого пути `File.Dest`, затем обращаться к индексу дополнительных библиотек.
  - [x] Проверять наличие копии до добавления нового удалённого файла в JSON-очередь.
  - [x] Повторять проверку непосредственно перед началом уже поставленной в очередь загрузки.
  - [x] Удалять пропущенную задачу из очереди, если локальная копия была подтверждена перед загрузкой.
  - [x] Не начинать range-запрос и не создавать временные части для подтверждённой локальной копии.

- [x] Согласовать найденные копии с безопасным удалением на WebDAV
  - [x] При выключенном `CLEAR_REMOTE` только пропускать загрузку, не изменяя удалённый файл.
  - [x] При включённом `CLEAR_REMOTE` повторно подтверждать локальный путь и размер непосредственно перед вызовом `Downloader.Delete`.
  - [x] Не удалять удалённый файл, если повторная проверка не нашла ни одной актуальной локальной копии.
  - [x] При ошибке WebDAV `DELETE` оставлять удалённый файл доступным для повторной попытки на следующем сканировании.
  - [x] Применить тот же источник локальных совпадений к одноразовому режиму `UTIL_CLEAR_REMOTE`, сохранив обязательную финальную проверку перед каждым удалением.

- [x] Покрыть индекс и интеграцию автоматическими тестами
  - [x] Проверить рекурсивное обнаружение файлов при различающихся относительных путях.
  - [x] Проверить точное совпадение имени с учётом регистра.
  - [x] Проверить точное совпадение размера.
  - [x] Проверить несколько локальных путей для одного ключа.
  - [x] Проверить исчезновение и изменение файла после построения индекса.
  - [x] Проверить атомарную замену успешного снимка.
  - [x] Проверить сохранение предыдущего снимка после ошибки периодического обновления.
  - [x] Через fake-реализации `Scanner`, `Downloader` и `Queue` проверить пропуск загрузки до постановки в очередь.
  - [x] Проверить удаление существующей задачи из очереди без запуска загрузчика.
  - [x] Проверить отсутствие WebDAV-удаления при выключенном `CLEAR_REMOTE`.
  - [x] Проверить WebDAV-удаление при включённом `CLEAR_REMOTE` и актуальной локальной копии.
  - [x] Проверить запрет WebDAV-удаления при устаревшей записи индекса.

- [x] Обновить эксплуатационную документацию
  - [x] Добавить новые временные переменные окружения и их значения по умолчанию в `example.env`.
  - [x] Описать в `README.md` монтирование нескольких каталогов в контейнер с флагом `:ro`.
  - [x] Привести пример настройки нескольких путей и суточного интервала обновления.
  - [x] Уточнить, что совпадение определяется только регистрозависимым именем и размером, а не содержимым, путём или временем изменения.
  - [x] Предупредить, что `CLEAR_REMOTE` и `UTIL_CLEAR_REMOTE` физически удаляют подтверждённые совпадения с WebDAV.

- [x] Проверить готовое изменение
  - [x] Отформатировать изменённые Go-файлы через `gofmt`.
  - [x] Выполнить `go test ./...`.
  - [x] Выполнить `go vet ./...`.
  - [x] Собрать CLI командой `go build -o /tmp/wddl ./cmd/webdav`.
  - [x] В контейнере примонтировать тестовую библиотеку read-only и подтвердить пропуск файла с другим локальным путём.
  - [x] В тестовом WebDAV-каталоге отдельно проверить оба режима `CLEAR_REMOTE` без использования production-данных.
