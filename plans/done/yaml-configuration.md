---
Название: "Перенос конфигурации приложения в YAML"
Сложность: medium
Важность: 2
Дата завершения: 2026-09-13
---

## Что мы делаем?

Переносим постоянные настройки `wddl` из набора CLI-флагов и переменных окружения в один версионированный YAML-файл. Вне YAML остаются только путь к самому файлу, `WEBDAV_USER`, `WEBDAV_PASSWORD` и выбираемое пользователем действие CLI. Переход выполняется как намеренно несовместимое изменение для `v1.0`, без поддержки старых переменных `INPUT`, `OUTPUT`, `THREADS`, `TIMEOUT`, `DB_FILE` и других удаляемых параметров.

## Для чего мы делаем?

Текущая плоская конфигурация становится неудобной с появлением JSON-очереди, нескольких дополнительных локальных библиотек и отдельных интервалов сканирования. YAML позволит сгруппировать связанные настройки, задавать списки путей без строкового кодирования и явно указывать интервалы вроде `10m` и `24h`. Отделение credentials от файла не даст случайно сохранить логин или пароль в репозитории и останется удобным для Docker secrets и переменных окружения.

## План

- [x] Зафиксировать версионированную схему YAML
  - [x] Добавить обязательное верхнеуровневое поле `version` со значением `1`.
  - [x] Добавить секцию `webdav` с полями `server` и `root`, не допуская полей логина или пароля.
  - [x] Добавить секцию `download` с полями `destination`, `temp`, `workers`, `scan_every` и `remove_remote`.
  - [x] Добавить секцию `queue` с полем `file` для запланированного JSON-хранилища.
  - [x] Добавить секцию `existing_files` с массивом `roots` и полем `scan_every` для запланированного локального индекса.
  - [x] Добавить секцию `logging` с полем `debug`.
  - [x] Не включать в схему одноразовые действия запуска, включая очистку удалённого хранилища.

- [x] Создать пакет загрузки конфигурации
  - [x] Добавить типизированные структуры YAML, отделённые от `engine.Config` и CLI bootstrap-параметров.
  - [x] Сделать `gopkg.in/yaml.v3` прямой зависимостью проекта.
  - [x] Применять значения по умолчанию до декодирования пользовательского файла.
  - [x] Декодировать YAML в строгом режиме и отклонять неизвестные поля.
  - [x] Преобразовывать строковые интервалы через `time.ParseDuration`.
  - [x] Возвращать ошибки с указанием проблемной секции или поля без вывода чувствительных данных.

- [x] Определить значения по умолчанию
  - [x] Использовать `/` для `webdav.root`.
  - [x] Использовать `./download` для `download.destination`.
  - [x] Использовать `/tmp/wddl` для `download.temp`.
  - [x] Использовать `4` для `download.workers`.
  - [x] Использовать `10m` для `download.scan_every`.
  - [x] Оставить `download.remove_remote` выключенным.
  - [x] Использовать `./queue.json` для `queue.file`.
  - [x] Использовать пустой список для `existing_files.roots`.
  - [x] Использовать `24h` для `existing_files.scan_every`.
  - [x] Оставить `logging.debug` выключенным.

- [x] Добавить строгую валидацию загруженной конфигурации
  - [x] Проверять поддерживаемую версию схемы.
  - [x] Проверять непустой HTTP(S)-адрес `webdav.server`.
  - [x] Проверять абсолютный WebDAV-путь `webdav.root` с `/` в качестве разделителя.
  - [x] Проверять непустые локальные пути назначения, временных файлов и очереди.
  - [x] Запрещать совпадение `download.destination` и `download.temp` после очистки локальных путей.
  - [x] Требовать положительное значение `download.workers`.
  - [x] Требовать положительные значения обоих интервалов сканирования.
  - [x] Нормализовать список `existing_files.roots`, удаляя пустые и повторяющиеся пути.
  - [x] Проверять, что каждый настроенный корень локальной библиотеки существует и доступен только на этапе запуска соответствующего индекса.

- [x] Оставить bootstrap-настройки вне YAML
  - [x] Добавить CLI-флаг `--config` для явного пути к файлу.
  - [x] Добавить `WDDL_CONFIG` как альтернативный источник пути к файлу.
  - [x] Установить приоритет `--config`, затем `WDDL_CONFIG`, затем `./config.yaml`.
  - [x] Читать имя пользователя только из `WEBDAV_USER`.
  - [x] Читать пароль только из `WEBDAV_PASSWORD`.
  - [x] Завершать запуск с понятной ошибкой при отсутствии обязательных credentials.
  - [x] Не добавлять общие override-переменные окружения для значений из YAML.

- [x] Перестроить порядок инициализации `cmd/webdav/main.go`
  - [x] Сначала разобрать только путь к YAML, credentials и выбранное действие CLI.
  - [x] Затем полностью загрузить и проверить YAML до подключения к WebDAV.
  - [x] Настроить debug-логирование из `logging.debug` до создания основных компонентов приложения.
  - [x] Не возвращать переменную `DEBUG` как скрытый второй источник настройки логирования.
  - [x] Преобразовать секции YAML в `engine.Config`, настройки JSON-очереди и настройки локального индекса в одном явном месте композиции зависимостей.
  - [x] Сохранить credentials в отдельной структуре, которая не сериализуется в YAML и не печатается целиком в логах.
  - [x] Изолировать выбор команды от загрузки конфигурации, чтобы следующий план мог заменить `UTIL_CLEAR_REMOTE` полноценной CLI-командой без изменения схемы YAML.

- [x] Удалить старую конфигурационную поверхность
  - [x] Удалить флаги `--input`, `--temp`, `--output`, `--db-file`, `--threads`, `--timeout` и `--clear-remote` из постоянной конфигурации приложения.
  - [x] Удалить обработку переменных `INPUT`, `TEMP`, `OUTPUT`, `DB_FILE`, `THREADS`, `TIMEOUT`, `CLEAR_REMOTE` и `DEBUG`.
  - [x] Удалить вложенные CLI-параметры `--webdav.server` и связанные с ними старые теги конфигурации.
  - [x] Не добавлять предупреждения или fallback для старых имён, поскольку переход к `v1.0` намеренно ломает совместимость.
  - [x] Оставить временный механизм выбора действия до выполнения отдельного плана CLI-команд.

- [x] Добавить пример конфигурации
  - [x] Создать `config.example.yaml` со всеми секциями и безопасными значениями.
  - [x] Показать в примере несколько `existing_files.roots`.
  - [x] Не добавлять в пример поля username или password.
  - [x] Не добавлять в пример выбор действия очистки.
  - [x] Удалить устаревший `example.env` после переноса полезных пояснений.

- [x] Покрыть загрузчик конфигурации тестами
  - [x] Проверить загрузку полного корректного YAML.
  - [x] Проверить применение каждого значения по умолчанию к сокращённому YAML.
  - [x] Проверить отказ при неизвестном поле.
  - [x] Проверить отказ при неизвестной версии схемы.
  - [x] Проверить ошибки для пустого или синтаксически повреждённого файла.
  - [x] Проверить разбор корректных интервалов duration.
  - [x] Проверить отказ для нулевых, отрицательных и некорректных интервалов.
  - [x] Проверить валидацию URL, WebDAV-пути, локальных путей и числа воркеров.
  - [x] Проверить нормализацию повторяющихся корней локальных библиотек.
  - [x] Проверить приоритет CLI-пути над `WDDL_CONFIG`.
  - [x] Проверить отсутствие credentials в десериализуемой схеме.
  - [x] Проверить преобразование конфигурации в зависимости движка без обращения к настоящему WebDAV.

- [x] Обновить контейнерную конфигурацию и документацию
  - [x] Оставить в `Dockerfile` только `WDDL_CONFIG`, `WEBDAV_USER` и `WEBDAV_PASSWORD` из переменных, относящихся к настройке приложения.
  - [x] Описать read-only монтирование YAML-файла в контейнер.
  - [x] Описать отдельное постоянное монтирование каталогов `download`, `temp` и файла очереди в соответствии с путями из YAML.
  - [x] Обновить `README.md` примером запуска контейнера с YAML и credentials из окружения.
  - [x] Описать схему, значения по умолчанию и формат интервалов в `README.md`.
  - [x] Обновить конфигурационный раздел `AGENTS.md` и убрать ссылки на удалённые переменные окружения.
  - [x] Подчеркнуть разрушительное поведение `download.remove_remote: true`.

- [x] Проверить готовое изменение
  - [x] Отформатировать изменённые Go-файлы через `gofmt`.
  - [x] Выполнить `go mod tidy` и проверить ожидаемый diff зависимостей.
  - [x] Выполнить `go test ./...`.
  - [x] Выполнить `go test -race ./...` после выполнения плана замены BoltDB.
  - [x] Выполнить `go vet ./...`.
  - [x] Собрать CLI командой `go build -o /tmp/wddl ./cmd/webdav`.
  - [x] Запустить тестовый контейнер с read-only YAML и credentials из окружения.
  - [x] Проверить понятную ошибку запуска без YAML.
  - [x] Проверить понятную ошибку запуска без каждого обязательного credential.
  - [x] Проверить, что устаревшие переменные окружения больше не меняют поведение приложения.
