# wddl

Односторонняя утилита для загрузки файлов с WebDAV на локальный диск. Она не
требует `POST` или загрузки данных на сервер и подходит для WebDAV-интерфейса
MyPikPak.

## Конфигурация YAML

Постоянные настройки читаются из одного YAML-файла. Путь выбирается в порядке
`--config`, `WDDL_CONFIG`, `./config.yaml`. В Docker-образе `WDDL_CONFIG` по
умолчанию равен `/config/config.yaml`; монтируйте этот файл только для чтения.
Логин и пароль намеренно отсутствуют в YAML и читаются только из
`WEBDAV_USER` и `WEBDAV_PASSWORD`.

Полный пример находится в `config.example.yaml`. Схема версии `1` содержит:

- `webdav.server` и `webdav.root`;
- `download.destination`, `download.temp`, `download.workers`,
  `download.scan_every`, `download.remove_remote`;
- `queue.file`;
- `existing_files.roots`, `existing_files.scan_every`;
- `logging.debug`.

Значения интервалов используют синтаксис Go duration: `30s`, `10m`, `24h`.
Если поле не задано, применяются значения `/`, `./download`, `/tmp/wddl`, `4`,
`10m`, `false`, `./queue.json`, пустой список библиотек, `24h` и `false`
соответственно. `version`, `webdav.server`, `WEBDAV_USER` и
`WEBDAV_PASSWORD` обязательны.

Старые переменные `INPUT`, `OUTPUT`, `TEMP`, `THREADS`, `TIMEOUT`, `QUEUE_FILE`,
`CLEAR_REMOTE`, `DEBUG` и соответствующие CLI-флаги больше не используются.
Это намеренно несовместимое изменение для версии 1.0.

## Запуск контейнера

Скопируйте `config.example.yaml` в `config.yaml`, укажите WebDAV-сервер и пути,
затем запустите контейнер. Конфигурация и медиабиблиотеки монтируются read-only;
каталоги загрузок, временных частей и состояния — отдельными writable mounts.

```sh
docker run --name wddl \
  -e WDDL_CONFIG=/config/config.yaml \
  -e WEBDAV_USER=user \
  -e WEBDAV_PASSWORD=password \
  -v ./config.yaml:/config/config.yaml:ro \
  -v /volume1/wddl/download:/data/download \
  -v /volume1/wddl/tmp:/data/tmp \
  -v /volume1/wddl/state:/data/state \
  -v /volume1/video/movies:/libraries/movies:ro \
  -v /volume1/video/archive:/libraries/archive:ro \
  wddl
```

Пути внутри контейнера должны совпадать с `download.destination`,
`download.temp`, `queue.file` и `existing_files.roots` в YAML.

## Дополнительные локальные библиотеки

`existing_files.roots` позволяет учитывать файлы, которые уже перенесены из
каталога загрузок в другие медиабиблиотеки. При запуске все корни рекурсивно
сканируются до подключения движка загрузок. Индекс содержит только метаданные и
полностью заменяется после успешного повторного обхода с интервалом
`existing_files.scan_every`, по умолчанию `24h`. При ошибке предыдущий снимок
остаётся активным.

Совпадением считается только точная регистрозависимая пара «имя файла + размер
в байтах». Путь, время изменения и содержимое не сравниваются. Перед каждым
положительным ответом реальный файл повторно проверяется через `stat`. Без
дополнительных корней готовый файл ищется только по ожидаемому пути внутри
`download.destination`.

## Очередь загрузок

Незавершённые задачи хранятся в памяти процесса и после каждого изменения
атомарно сохраняются в читаемом версионированном JSON-файле. Путь задаётся через
`queue.file` и по умолчанию равен `./queue.json`. Размещайте каталог очереди на
постоянном writable volume, если состояние должно переживать пересоздание
контейнера.

Один файл очереди предназначен только для одного экземпляра `wddl`. Не
редактируйте и не удаляйте его во время работы. Пустой, повреждённый или
созданный более новой несовместимой версией файл не перезаписывается: приложение
завершит запуск с ошибкой.

`queue.json` можно удалить при остановленном контейнере. Задачи восстановятся во
время следующего сканирования WebDAV. Корректные части находятся отдельно в
`download.temp/<file-id>` и будут использованы для продолжения загрузки.

## Удаление с WebDAV

`download.remove_remote: true` и временный одноразовый режим
`UTIL_CLEAR_REMOTE=true` физически удаляют с WebDAV файлы, для которых найдена
локальная копия. Перед каждым удалением путь, имя и размер локального файла
проверяются повторно. Оставляйте оба режима выключенными, если удалённое
хранилище должно оставаться неизменным.
